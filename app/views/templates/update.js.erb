$('div.alert').remove();
window.scrollTo(0,0);
var type, message;
if("<%= raw @saved %>" === "true"){
  type = "success"
  message = "<b> Success - </b> Template succesfully updated"
} else {
  type = "danger"
  var template = "<%= raw @template.errors.messages[:invalid].first %> "
  message = `Ooops! - invalid notation detected on <b style='font-size: larger;'>${template}</b>, change it the correct format or value to continue`
}

alerts.showNotification(type, message)
